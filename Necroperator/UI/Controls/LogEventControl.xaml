<UserControl x:Class="Necroperator.UI.Controls.LogEventControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:logging="clr-namespace:Microsoft.Extensions.Logging;assembly=Microsoft.Extensions.Logging.Abstractions"
             mc:Ignorable="d" 
             MinHeight="10"
             MinWidth="100"
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Foreground" Value="#b0b0b0"/>
            </Style>

            <Style TargetType="TextBlock" x:Key="TextBlock_LogLevel">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Foreground" Value="#EFEFEF"/>
                <Setter Property="VerticalAlignment" Value="Center" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=LogLevel}" Value="{x:Static logging:LogLevel.Information}" >
                        <Setter Property="Foreground" Value="#0066ff" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=LogLevel}" Value="{x:Static logging:LogLevel.Warning}" >
                        <Setter Property="Foreground" Value="#e68a00" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=LogLevel}" Value="{x:Static logging:LogLevel.Error}" >
                        <Setter Property="Foreground" Value="#cc0000" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=LogLevel}" Value="{x:Static logging:LogLevel.Critical}" >
                        <Setter Property="Foreground" Value="#cc0000" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Timestamp, StringFormat=hh\\:mm\\:ss}" />
        <TextBlock Grid.Column="1" Margin="4,0,4,0" Style="{StaticResource TextBlock_LogLevel}">
            <TextBlock.Inlines>
                <Run Text="[" />
                <Run Text="{Binding LogLevel}" />
                <Run Text="]" />
            </TextBlock.Inlines>
        </TextBlock>
        <TextBlock Grid.Column="2" Text="{Binding Message}"/>
    </Grid>
</UserControl>
