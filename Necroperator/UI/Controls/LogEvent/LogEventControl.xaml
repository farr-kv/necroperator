<UserControl x:Class="Necroperator.UI.Controls.LogEvent.LogEventControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="10"
             MinWidth="100"
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UI/Controls/LogEvent/LogEventControlStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Timestamp, StringFormat=hh\\:mm\\:ss}" />
        <TextBlock Grid.Column="1" Margin="4,0,4,0" Style="{StaticResource TextBlock_LogLevel}">
            <TextBlock.Inlines>
                <Run Text="[" />
                <Run Text="{Binding LogLevel}" />
                <Run Text="]" />
            </TextBlock.Inlines>
        </TextBlock>
        <TextBlock Grid.Column="2" Text="{Binding Message}"/>
    </Grid>
</UserControl>
